# Copy to .env and fill in values for your environment.
# The orchestrator/user should provide these values (do not hardcode in code).

# Base URL of the notes backend REST API (no trailing slash recommended)
# Example: http://10.0.2.2:8000  (Android emulator -> host localhost)
API_BASE_URL=http://localhost:8000
